import { Modal } from 'src/components/feedback/Modal/Modal'
import { type Meta } from '@storybook/react'

const meta: Meta<typeof Modal> = {
  title: 'Components/Feedback/Modal',
  component: Modal,
  args: {},
}
export default meta

// This file serves as the base component export for Modal
// Individual examples are in the subfolders: Primary and Delete Confirm
