---
description: End-to-end ticket implementation — from Jira to PR in one command
argument-hint: <TICKET-ID>
---

Invoke the **implement-ticket** skill.

Implement ticket end-to-end: $ARGUMENTS

Fetch the Jira ticket, create a branch from main, investigate the codebase, implement the changes, verify build + types, commit, push, and create a PR with Jira link.
