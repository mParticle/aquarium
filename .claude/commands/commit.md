---
description: Stage and commit changes with conventional commit message
---

Invoke the **conventional-commit** skill to analyze current changes and create an atomic commit.

Analyze staged and unstaged changes, determine the commit type and scope, generate a conventional commit message, and execute the commit. If changes span multiple concerns, recommend splitting into separate commits.
